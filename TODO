Add multiple survey support
(done) new column in users table, update test.sql
(done) config.php - new array keys = survey name, values = image directory
(make convenience list) admin page for switching surveys into flat file and ensuring proper db setup 
(done) start.php - submission of new user needs to include survey, if unset, set.
(done) helpers.php inclusion of get_string_from_file and set_string_to_file