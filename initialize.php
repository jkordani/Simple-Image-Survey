find count of images in directory ./survey-images

read filenames into array

build big insert query to update images table

submit query

generate random ordering file newline delimited using count, later recall images during quiz with id (not picture_id, as this supports any filename for images)
